spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rideshare_users
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: local-dev-test-secret-do-not-use-in-production
